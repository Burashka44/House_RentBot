# Интеграция с платежными системами для оплаты ЖКХ

## Обзор

HouseBot поддерживает автоматическую генерацию ссылок на оплату коммунальных услуг через:
- **ЮMoney** (Яндекс.Деньги)
- **QIWI**
- **SberPay**
- **Т-Банк**

---

## 🏗️ Архитектура данных

### **1. РСО (Ресурсоснабжающая организация)**
Хранится в таблице `comm_providers`:

```python
class CommProvider:
    id: int
    name: str               # "ПАО Мосэнерго"
    service_type: str       # "Электроснабжение"
    
    # Новые поля для платежей:
    inn: str                # ИНН РСО
    bik: str                # БИК банка
    bank_account: str       # Номер счета
    payment_purpose_template: str  # Шаблон назначения платежа
    yoomoney_service_id: str      # ID услуги в ЮMoney (если есть)
```

### **2. Лицевой счет по квартире**
Привязка РСО к конкретной квартире в `object_rso_links`:

```python
class ObjectRSOLink:
    id: int
    object_id: int          # ID квартиры
    provider_id: int        # ID РСО
    
    # Данные для платежей:
    personal_account: str   # Лицевой счет (номер абонента)
    contract_number: str    # Номер договора
    service_code: str       # Код услуги (для API)
    payment_data: JSON      # Дополнительные данные (адрес плательщика и т.д.)
```

---

## 💳 Workflow: Оплата через ЮMoney

### **Шаг 1: Настройка РСО**

Админ в боте:
1. Добавляет дом
2. Добавляет РСО (электричество, вода, газ...)
3. Заполняет реквизиты:
   - ИНН РСО
   - БИК банка (опционально)
   - Банковский счет (опционально)

### **Шаг 2: Настройка квартиры**

Админ:
1. Создает квартиру (объект аренды)
2. Привязывает РСО к квартире
3. **Вводит лицевые счета** для каждого РСО
   - Электричество: `12345678`
   - Вода: `87654321`
   - Газ: `11223344`

### **Шаг 3: Генерация ссылок на оплату**

Жилец в боте:
1. Нажимает "💰 Оплатить ЖКХ"
2. Выбирает период (например, "Январь 2026")
3. Бот показывает список услуг с суммами:
   ```
   Электричество: 1,200₽ [Оплатить ЮMoney]
   Водоснабжение: 800₽  [Оплатить ЮMoney]
   Газ: 600₽            [Оплатить ЮMoney]
   ```
4. Жилец нажимает кнопку → переходит на страницу оплаты ЮMoney
5. Оплачивает картой
6. Бот получает уведомление (webhook) → помечает как оплачено

---

## 🔧 Техническая реализация

### **1. Миграция БД**

Выполнить миграцию для добавления полей:
```bash
alembic upgrade head
```

### **2. Настройка ЮMoney**

Регистрация:
1. Зарегистрировать приложение на https://yoomoney.ru/myservices/
2. Получить `client_id` и `client_secret`
3. (Опционально) Получить `access_token` для автоплатежей

Добавить в `.env`:
```
YOOMONEY_TOKEN=your_access_token_here
YOOMONEY_CLIENT_ID=your_client_id
```

### **3. Использование в боте**

Пример хендлера:
```python
from bot.services.yoomoney_service import generate_payment_links_for_tenant

@router.callback_query(F.data == "pay_utilities")
async def pay_utilities(call: CallbackQuery, tenant, session: AsyncSession):
    # Генерируем ссылки на оплату
    links = await generate_payment_links_for_tenant(
        session=session,
        tenant_id=tenant.id,
        period="2026-01"
    )
    
    if not links:
        await call.message.answer("❌ Нет активных начислений")
        return
    
    # Показываем кнопки
    keyboard = []
    for link in links:
        keyboard.append([
            InlineKeyboardButton(
                text=f"{link.service_type}: {link.amount}₽",
                url=link.url
            )
        ])
    
    kb = InlineKeyboardMarkup(inline_keyboard=keyboard)
    await call.message.answer("💰 Выберите услугу для оплаты:", reply_markup=kb)
```

---

## 📊 Пример данных

### **Пример РСО (ПАО "Мосэнерго")**
```json
{
  "id": 1,
  "name": "ПАО Мосэнерго",
  "service_type": "Электроснабжение",
  "inn": "7728022156",
  "bik": "044525225",
  "bank_account": "40702810838000012345",
  "yoomoney_service_id": "electric_moscow"
}
```

### **Пример лицевого счета**
```json
{
  "id": 1,
  "object_id": 5,
  "provider_id": 1,
  "personal_account": "012345678901",
  "contract_number": "МСЭ-123456",
  "payment_data": {
    "payer_address": "г. Москва, ул. Ленина, д. 5, кв. 10",
    "payer_name": "Иванов Иван Иванович"
  }
}
```

---

## 🔗 API ЮMoney

### **Создание платежной формы**

URL:
```
https://yoomoney.ru/quickpay/confirm?receiver=ИНН&sum=1500&label=ЛС123&targets=ОплатаЖКХ
```

Параметры:
- `receiver` - ИНН получателя (РСО)
- `sum` - Сумма платежа
- `label` - Идентификатор (лицевой счет)
- `targets` - Назначение платежа
- `paymentType` - Тип оплаты (`AC` = карта)

### **Проверка статуса платежа**

Endpoint: `POST https://yoomoney.ru/api/operation-history`

Headers:
```
Authorization: Bearer YOUR_ACCESS_TOKEN
```

Body:
```json
{
  "label": "ЛС123"
}
```

Response:
```json
{
  "operations": [
    {
      "operation_id": "123",
      "status": "success",
      "amount": 1500.00,
      "datetime": "2026-01-10T12:00:00Z"
    }
  ]
}
```

---

## 🎯 Roadmap

### **Этап 1: MVP** (Текущий)
- [x] Модели БД для РСО и лицевых счетов
- [x] YooMoney сервис (генерация ссылок)
- [ ] Миграция БД
- [ ] Админ-интерфейс для ввода лицевых счетов
- [ ] Кнопка "Оплатить ЖКХ" в меню жильца

### **Этап 2: Автоматизация**
- [ ] Webhook от ЮMoney для автоподтверждения
- [ ] Автоматический расчет сумм из начислений
- [ ] Интеграция с ГИС ЖКХ для автозаполнения лицевых счетов

### **Этап 3: Расширение**
- [ ] QIWI интеграция
- [ ] SberPay
- [ ] Т-Банк
- [ ] Рекуррентные платежи (автоплатеж)

---

## ❓ FAQ

**Q: Можно ли платить без комиссии?**  
A: Зависит от РСО. Некоторые покрывают комиссию, некоторые - нет.

**Q: Как узнать лицевой счет?**  
A: Из квитанции или из ГИС ЖКХ API.

**Q: Нужна ли регистрация в ЮMoney?**  
A: Для создания ссылок - нет. Для webhook и проверки статуса - да.

**Q: Можно ли автоматически списывать с кошелька?**  
A: Да, но требуется `access_token` и согласие пользователя.

---

## 🔐 Безопасность

1. **Не храните** полные данные карт
2. **Используйте HTTPS** для webhook
3. **Проверяйте подписи** от платежных систем
4. **Логируйте** все транзакции

---

## 📱 UI для жильца

```
💰 Оплата ЖКХ за Январь 2026

┌─────────────────────────────┐
│ ⚡ Электроснабжение          │
│ Лицевой счет: 012345678901  │
│ К оплате: 1,200₽           │
│ [💳 Оплатить ЮMoney]        │
└─────────────────────────────┘

┌─────────────────────────────┐
│ 💧 Водоснабжение            │
│ Лицевой счет: 987654321000  │
│ К оплате: 800₽             │
│ [💳 Оплатить ЮMoney]        │
└─────────────────────────────┘

┌─────────────────────────────┐
│ 🔥 Отопление                │
│ Лицевой счет: 555666777888  │
│ К оплате: 2,500₽           │
│ [💳 Оплатить ЮMoney]        │
└─────────────────────────────┘

────────────────────────────────
Итого: 4,500₽
```
